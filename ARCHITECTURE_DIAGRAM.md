# 🏗️ ARCHITECTURE OVERVIEW - Radio ISTIC Dashboard

## Current Architecture (After Fixes)

```
┌─────────────────────────────────────────────────────────────────────┐
│                         PRODUCTION DEPLOYMENT                        │
└─────────────────────────────────────────────────────────────────────┘

┌────────────────────────┐         ┌────────────────────────────────┐
│   FRONTEND (Netlify)   │         │    BACKEND (Render.com)        │
│  ┌──────────────────┐  │         │  ┌──────────────────────────┐  │
│  │   Next.js 14.2   │  │ HTTPS   │  │   Express 4.18.2         │  │
│  │   React 18       │◄─┼─────────┼─►│   Node.js                │  │
│  │   TypeScript 5   │  │  API    │  │   Port: 5000             │  │
│  │   Tailwind v4    │  │ Calls   │  │                          │  │
│  └────────┬─────────┘  │         │  └────────┬─────────────────┘  │
│           │            │         │           │                     │
│           │ Socket.IO  │ WSS     │  ┌────────▼─────────────────┐  │
│           │ Client     ◄─┼─────────┼─►│  Socket.IO Server       │  │
│           │            │         │  │  JWT Auth                │  │
│  Port: 3000            │         │  │  Online Users Tracking   │  │
└────────────────────────┘         │  └──────────────────────────┘  │
                                   │                                 │
                                   │  ┌──────────────────────────┐  │
                                   │  │  REST API Routes         │  │
                                   │  │  - /api/auth/*           │  │
                                   │  │  - /api/users/*          │  │
                                   │  │  - /api/events/*         │  │
                                   │  │  - /api/health           │  │
                                   │  └──────────────────────────┘  │
                                   │                                 │
                                   │  ┌──────────────────────────┐  │
                                   │  │  Middleware              │  │
                                   │  │  - JWT Auth              │  │
                                   │  │  - Rate Limiting         │  │
                                   │  │  - Helmet Security       │  │
                                   │  │  - CORS                  │  │
                                   │  │  - Upload Validation ✅  │  │
                                   │  └──────────────────────────┘  │
                                   └─────────────┬───────────────────┘
                                                 │ Mongoose
                                                 │ ODM
                                                 ▼
                                   ┌─────────────────────────────────┐
                                   │   MongoDB Atlas (Cloud)         │
                                   │                                 │
                                   │  Collections:                   │
                                   │  ├─ users (46 documents)        │
                                   │  ├─ events (6 documents)        │
                                   │  ├─ conversations (4 docs)      │
                                   │  └─ messages (18 documents)     │
                                   │                                 │
                                   │  Region: Closest to users       │
                                   │  Backup: Automatic              │
                                   └─────────────────────────────────┘
```

---

## Data Flow - Authentication

```
┌──────────┐         ┌──────────┐         ┌──────────┐         ┌──────────┐
│  User    │         │ Frontend │         │ Backend  │         │ MongoDB  │
│ Browser  │         │ (Netlify)│         │ (Render) │         │  Atlas   │
└────┬─────┘         └────┬─────┘         └────┬─────┘         └────┬─────┘
     │                    │                    │                    │
     │ 1. POST /signup    │                    │                    │
     ├───────────────────►│                    │                    │
     │                    │ 2. POST            │                    │
     │                    │    /api/auth/      │                    │
     │                    │    register        │                    │
     │                    ├───────────────────►│                    │
     │                    │                    │ 3. Hash password   │
     │                    │                    │    (bcrypt)        │
     │                    │                    │                    │
     │                    │                    │ 4. Save user       │
     │                    │                    ├───────────────────►│
     │                    │                    │                    │
     │                    │                    │ 5. User created    │
     │                    │                    │◄───────────────────┤
     │                    │                    │                    │
     │                    │                    │ 6. Generate JWT    │
     │                    │                    │    token           │
     │                    │                    │                    │
     │                    │ 7. Return          │                    │
     │                    │    {token, user}   │                    │
     │                    │◄───────────────────┤                    │
     │                    │                    │                    │
     │ 8. Store token     │                    │                    │
     │    in localStorage │                    │                    │
     │◄───────────────────┤                    │                    │
     │                    │                    │                    │
     │ 9. Redirect to     │                    │                    │
     │    /members        │                    │                    │
     │◄───────────────────┤                    │                    │
     │                    │                    │                    │
```

---

## Data Flow - Real-Time Chat

```
┌──────────┐         ┌──────────┐         ┌──────────┐         ┌──────────┐
│  User A  │         │ Frontend │         │ Backend  │         │ MongoDB  │
└────┬─────┘         └────┬─────┘         └────┬─────┘         └────┬─────┘
     │                    │                    │                    │
     │ 1. Open /chat      │                    │                    │
     ├───────────────────►│                    │                    │
     │                    │                    │                    │
     │                    │ 2. Socket.IO       │                    │
     │                    │    connect         │                    │
     │                    │    + JWT token     │                    │
     │                    ├───────────────────►│                    │
     │                    │                    │ 3. Verify JWT      │
     │                    │                    │                    │
     │                    │                    │ 4. Get user info   │
     │                    │                    ├───────────────────►│
     │                    │                    │◄───────────────────┤
     │                    │                    │                    │
     │                    │ 5. Connection      │                    │
     │                    │    established     │                    │
     │                    │◄───────────────────┤                    │
     │                    │                    │                    │
     │ 6. Type message    │                    │                    │
     ├───────────────────►│                    │                    │
     │                    │                    │                    │
     │                    │ 7. Emit            │                    │
     │                    │    'send-message'  │                    │
     │                    ├───────────────────►│                    │
     │                    │                    │ 8. Save message    │
     │                    │                    ├───────────────────►│
     │                    │                    │◄───────────────────┤
     │                    │                    │                    │
     │                    │                    │ 9. Emit to         │
     │                    │                    │    recipient       │
     │                    │                    │    (User B)        │
     │                    │                    │                    │
     │                    │ ┌──────────────────┤                    │
     │                    │ │10. 'new-message' │                    │
     │ User B receives ───┼─┤  event           │                    │
     │ in real-time       │ │                  │                    │
     │                    │ └──────────────────┤                    │
     │                    │                    │                    │
```

---

## Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                     REQUEST FROM BROWSER                     │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
               ┌──────────────────────┐
               │   1. CORS Check       │
               │   ✓ Origin allowed    │
               └──────────┬────────────┘
                          │
                          ▼
               ┌──────────────────────┐
               │   2. Helmet Headers   │
               │   ✓ XSS Protection    │
               │   ✓ No Sniff          │
               │   ✓ Frame Guard       │
               └──────────┬────────────┘
                          │
                          ▼
               ┌──────────────────────┐
               │   3. Rate Limiting    │
               │   ✓ 100 req/15min     │
               │   ✓ 5 auth/15min      │
               │   ✓ 10 upload/hour ✅ │
               └──────────┬────────────┘
                          │
                          ▼
               ┌──────────────────────┐
               │   4. Input Sanitize   │
               │   ✓ NoSQL injection   │
               │   ✓ XSS clean         │
               └──────────┬────────────┘
                          │
                          ▼
               ┌──────────────────────┐
               │   5. JWT Auth         │
               │   ✓ Token valid       │
               │   ✓ Not expired       │
               │   ✓ Signature match   │
               └──────────┬────────────┘
                          │
                          ▼
               ┌──────────────────────┐
               │   6. Upload Validate  │ ✅ NEW
               │   ✓ File type         │
               │   ✓ File size         │
               │   ✓ MIME vs ext       │
               └──────────┬────────────┘
                          │
                          ▼
               ┌──────────────────────┐
               │   7. Business Logic   │
               │   ✓ Process request   │
               └──────────┬────────────┘
                          │
                          ▼
               ┌──────────────────────┐
               │   8. MongoDB Atlas    │
               │   ✓ Encrypted transit │
               │   ✓ IP whitelist      │
               │   ✓ Auth required     │
               └──────────┬────────────┘
                          │
                          ▼
               ┌──────────────────────┐
               │   9. Response         │
               │   ✓ Security headers  │
               │   ✓ HTTPS only        │
               └──────────────────────┘
```

---

## File Upload Flow (Current - Local Storage)

```
┌──────────┐         ┌──────────┐         ┌──────────┐         ┌──────────┐
│  User    │         │ Frontend │         │ Backend  │         │  Render  │
│ Browser  │         │          │         │  Multer  │         │Filesystem│
└────┬─────┘         └────┬─────┘         └────┬─────┘         └────┬─────┘
     │                    │                    │                    │
     │ 1. Select image    │                    │                    │
     │    (< 5MB)         │                    │                    │
     ├───────────────────►│                    │                    │
     │                    │                    │                    │
     │                    │ 2. POST /upload    │                    │
     │                    │    + JWT token     │                    │
     │                    │    + FormData      │                    │
     │                    ├───────────────────►│                    │
     │                    │                    │ 3. Rate limit ✅   │
     │                    │                    │    check           │
     │                    │                    │                    │
     │                    │                    │ 4. Validate ✅     │
     │                    │                    │    - Type          │
     │                    │                    │    - Size          │
     │                    │                    │    - MIME          │
     │                    │                    │                    │
     │                    │                    │ 5. Save file       │
     │                    │                    ├───────────────────►│
     │                    │                    │                    │
     │                    │                    │ 6. File saved      │
     │                    │                    │◄───────────────────┤
     │                    │                    │ ⚠️ LOST ON         │
     │                    │                    │    RESTART!        │
     │                    │                    │                    │
     │                    │ 7. Return URL      │                    │
     │                    │◄───────────────────┤                    │
     │                    │                    │                    │
     │ 8. Display avatar  │                    │                    │
     │◄───────────────────┤                    │                    │
     │                    │                    │                    │

⚠️ ISSUE: Render has EPHEMERAL filesystem → uploads lost on restart
✅ SOLUTION: Implement Cloudinary (cloud storage)
```

---

## File Upload Flow (Recommended - Cloudinary)

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  User    │    │ Frontend │    │ Backend  │    │Cloudinary│
└────┬─────┘    └────┬─────┘    └────┬─────┘    └────┬─────┘
     │               │               │               │
     │ 1. Select img │               │               │
     ├──────────────►│               │               │
     │               │ 2. Upload     │               │
     │               ├──────────────►│               │
     │               │               │ 3. Validate ✅│
     │               │               │               │
     │               │               │ 4. Upload     │
     │               │               ├──────────────►│
     │               │               │               │
     │               │               │ 5. URL + CDN  │
     │               │               │◄──────────────┤
     │               │               │ ✅ PERSISTS   │
     │               │               │ ✅ FAST CDN   │
     │               │               │               │
     │               │ 6. Return URL │               │
     │               │◄──────────────┤               │
     │ 7. Display    │               │               │
     │◄──────────────┤               │               │
     │               │               │               │

✅ PERMANENT: Files never lost
✅ FAST: CDN-delivered worldwide
✅ OPTIMIZED: Auto-compression & resize
```

---

## Database Schema (MongoDB)

```
┌─────────────────────────────────────────────────────────────┐
│                        USERS COLLECTION                      │
├─────────────────────────────────────────────────────────────┤
│ {                                                            │
│   _id: ObjectId,                                            │
│   firstName: String (required),                             │
│   lastName: String (required),                              │
│   email: String (unique, indexed),                          │
│   password: String (hashed, bcrypt, select: false),         │
│   phone: String,                                            │
│   avatar: String (URL),                                     │
│   field: Enum['GLSI', 'IRS', 'LISI', 'LAI', 'IOT', 'LT'],  │
│   year: Enum[1, 2, 3],                                      │
│   coordonation: String,                                     │
│   motivation: String,                                       │
│   projects: String,                                         │
│   skills: [String],                                         │
│   role: String (default: 'member'),                         │
│   isBureau: Boolean (default: false),                       │
│   points: Number (default: 0, indexed),                     │
│   status: Enum['active', 'inactive'],                       │
│   isActive: Boolean,                                        │
│   lastLogin: Date,                                          │
│   socketId: String,                                         │
│   createdAt: Date,                                          │
│   updatedAt: Date                                           │
│ }                                                            │
│                                                              │
│ Indexes:                                                     │
│ - email (unique) → Fast login lookup                        │
│ - points → Leaderboard/ranking                              │
│ - field + year → Member filtering                           │
│                                                              │
│ Virtual Fields:                                              │
│ - name → `${firstName} ${lastName}`                         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                     CONVERSATIONS COLLECTION                 │
├─────────────────────────────────────────────────────────────┤
│ {                                                            │
│   _id: ObjectId,                                            │
│   participants: [ObjectId] (ref: 'User'),                   │
│   lastMessage: ObjectId (ref: 'Message'),                   │
│   lastMessageAt: Date,                                      │
│   unreadCount: {                                            │
│     [userId]: Number                                        │
│   },                                                        │
│   createdAt: Date,                                          │
│   updatedAt: Date                                           │
│ }                                                            │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      MESSAGES COLLECTION                     │
├─────────────────────────────────────────────────────────────┤
│ {                                                            │
│   _id: ObjectId,                                            │
│   conversationId: ObjectId (ref: 'Conversation'),           │
│   senderId: ObjectId (ref: 'User'),                         │
│   content: String,                                          │
│   type: Enum['text', 'image', 'file', 'audio'],            │
│   status: Enum['sent', 'delivered', 'read'],               │
│   readBy: [ObjectId] (ref: 'User'),                        │
│   createdAt: Date,                                          │
│   updatedAt: Date                                           │
│ }                                                            │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                       EVENTS COLLECTION                      │
├─────────────────────────────────────────────────────────────┤
│ {                                                            │
│   _id: ObjectId,                                            │
│   title: String,                                            │
│   description: String,                                      │
│   date: Date,                                               │
│   location: String,                                         │
│   image: String (URL),                                      │
│   organizer: ObjectId (ref: 'User'),                        │
│   participants: [ObjectId] (ref: 'User'),                   │
│   maxParticipants: Number,                                  │
│   status: Enum['upcoming', 'ongoing', 'completed'],        │
│   createdAt: Date,                                          │
│   updatedAt: Date                                           │
│ }                                                            │
└─────────────────────────────────────────────────────────────┘
```

---

## WebSocket Events

```
CLIENT → SERVER:
├─ connection               → Initial connection with JWT
├─ join-conversation        → Join specific chat room
├─ leave-conversation       → Leave chat room
├─ send-message            → Send new message
├─ typing                  → User is typing
├─ stop-typing             → User stopped typing
├─ mark-as-read            → Mark messages as read
└─ disconnect              → User disconnects

SERVER → CLIENT:
├─ connect                 → Connection established
├─ authenticated           → JWT verified, user authenticated
├─ online-users            → List of currently online users
├─ user-online             → Specific user came online
├─ user-offline            → Specific user went offline
├─ new-message             → New message received
├─ message-status          → Message status updated (delivered/read)
├─ typing                  → Someone is typing
├─ stop-typing             → Someone stopped typing
├─ conversation-updated    → Conversation metadata changed
└─ error                   → Error occurred
```

---

## API Endpoints

```
┌─────────────────────────────────────────────────────────────┐
│                     AUTHENTICATION ROUTES                    │
├─────────────────────────────────────────────────────────────┤
│ POST   /api/auth/register         → Create new user         │
│ POST   /api/auth/login            → Login user              │
│ GET    /api/auth/me               → Get current user ✓      │
│ PUT    /api/auth/update-profile   → Update profile ✓        │
│ PUT    /api/auth/change-password  → Change password ✓       │
│ POST   /api/auth/upload-avatar    → Upload avatar ✓ ✅      │
│                                      (rate limited + valid)  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                        USER ROUTES                           │
├─────────────────────────────────────────────────────────────┤
│ GET    /api/users                 → Get all users ✓         │
│ GET    /api/users/:id             → Get user by ID ✓        │
│ PUT    /api/users/:id             → Update user ✓           │
│ DELETE /api/users/:id             → Delete user ✓           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                       EVENT ROUTES                           │
├─────────────────────────────────────────────────────────────┤
│ GET    /api/events                → Get all events          │
│ GET    /api/events/:id            → Get event by ID         │
│ POST   /api/events                → Create event ✓          │
│ PUT    /api/events/:id            → Update event ✓          │
│ DELETE /api/events/:id            → Delete event ✓          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      UTILITY ROUTES                          │
├─────────────────────────────────────────────────────────────┤
│ GET    /api/health                → Health check            │
└─────────────────────────────────────────────────────────────┘

Legend:
✓ = Protected (requires JWT)
✅ = Recently improved with security
```

---

## Environment Configuration

```
┌─────────────────────────────────────────────────────────────┐
│                      DEVELOPMENT (.env)                      │
├─────────────────────────────────────────────────────────────┤
│ Frontend:                                                    │
│   NEXT_PUBLIC_API_URL=http://localhost:5000/api             │
│   NEXT_PUBLIC_SOCKET_URL=http://localhost:5000 ✅ (fixed)   │
│   NEXT_PUBLIC_SITE_URL=http://localhost:3000                │
│                                                              │
│ Backend:                                                     │
│   MONGODB_URI=mongodb+srv://...                             │
│   JWT_SECRET=64-char-secret                                 │
│   JWT_EXPIRE=7d                                             │
│   NODE_ENV=development                                      │
│   PORT=5000                                                 │
│   CORS_ORIGIN=http://localhost:3000                         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    PRODUCTION (.env.production)              │
├─────────────────────────────────────────────────────────────┤
│ Frontend (Netlify):                                          │
│   NEXT_PUBLIC_API_URL=https://api.onrender.com/api          │
│   NEXT_PUBLIC_SOCKET_URL=https://api.onrender.com           │
│   NEXT_PUBLIC_SITE_URL=https://app.netlify.app              │
│                                                              │
│ Backend (Render):                                            │
│   MONGODB_URI=mongodb+srv://...                             │
│   JWT_SECRET=NEW-32-char-secret (generate new!)             │
│   JWT_EXPIRE=7d                                             │
│   NODE_ENV=production                                       │
│   PORT=5000                                                 │
│   CORS_ORIGIN=https://app.netlify.app                       │
└─────────────────────────────────────────────────────────────┘
```

---

## Performance Metrics (Expected)

```
┌─────────────────────────────────────────────────────────────┐
│                      FRONTEND (Netlify)                      │
├─────────────────────────────────────────────────────────────┤
│ First Load (Cold):      2-3 seconds                         │
│ Subsequent Loads:       < 1 second                          │
│ Time to Interactive:    < 3 seconds                         │
│ Largest Contentful:     < 2.5 seconds                       │
│ First Input Delay:      < 100ms                             │
│ Cumulative Layout:      < 0.1                               │
│                                                              │
│ Lighthouse Scores:                                           │
│   Performance:    70-85 (Good)                              │
│   Accessibility:  90+ (Excellent)                           │
│   Best Practices: 90+ (Excellent)                           │
│   SEO:            80+ (Good)                                │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      BACKEND (Render)                        │
├─────────────────────────────────────────────────────────────┤
│ API Response Time:      < 200ms (simple queries)            │
│                        < 500ms (complex queries)            │
│ WebSocket Latency:      < 100ms (same region)              │
│ Database Query:         < 50ms (indexed queries)            │
│                        < 200ms (aggregations)               │
│                                                              │
│ Concurrent Users:       100+ (Free tier)                    │
│                        500+ (Paid tier)                     │
│ Uptime:                99.9% (Render SLA)                   │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                     DATABASE (MongoDB Atlas)                 │
├─────────────────────────────────────────────────────────────┤
│ Query Performance:      < 50ms (with indexes)              │
│ Connection Time:        < 100ms                             │
│ Storage (Free):         512MB                               │
│ Connections (Free):     500 concurrent                      │
│ Backup:                Automatic (Atlas)                    │
│ Uptime:                99.95% (Atlas SLA)                   │
└─────────────────────────────────────────────────────────────┘
```

---

## Monitoring Dashboard (Recommended Setup)

```
┌─────────────────────────────────────────────────────────────┐
│                      UPTIME MONITORING                       │
│                      (UptimeRobot - Free)                    │
├─────────────────────────────────────────────────────────────┤
│ Monitor 1: https://app.netlify.app (5min checks)           │
│ Monitor 2: https://api.onrender.com/api/health (5min)      │
│                                                              │
│ Alerts: Email when down                                     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                       ERROR TRACKING                         │
│                      (Sentry - Free tier)                    │
├─────────────────────────────────────────────────────────────┤
│ Frontend: React error boundary + Sentry SDK                 │
│ Backend: Express error middleware + Sentry                  │
│                                                              │
│ Captures: JS errors, API errors, performance issues         │
│ Alerts: Email/Slack when critical                           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      DATABASE METRICS                        │
│                   (MongoDB Atlas - Built-in)                 │
├─────────────────────────────────────────────────────────────┤
│ Dashboard: Connections, Operations/sec, Storage             │
│ Performance: Slow query logs, index usage                   │
│                                                              │
│ Alerts: High connections, low storage, slow queries         │
└─────────────────────────────────────────────────────────────┘
```

---

## Deployment Timeline

```
Day 0 (Today):
├─ [30min] Read all audit documents
├─ [15min] Decide on MongoDB (keep it!)
├─ [30min] Apply remaining fixes
└─ [1hr]   Run local tests

Day 1 (Tomorrow):
├─ [15min] Deploy backend to Render
├─ [10min] Deploy frontend to Netlify
├─ [5min]  Connect them (CORS)
├─ [30min] Run production tests
└─ [15min] Setup monitoring

Day 2-7 (First Week):
├─ Monitor logs daily
├─ Fix any production issues
├─ Gather user feedback
└─ Document any problems

Week 2+:
├─ Implement Cloudinary
├─ Add refresh tokens
├─ Optimize performance
└─ Add new features
```

---

## Quick Commands Reference

```bash
# DEVELOPMENT
npm run dev                    # Start frontend (port 3000)
cd backend-api && npm start    # Start backend (port 5000)
npm run build                  # Build for production
npm run lint                   # Check code quality

# DATABASE
mongosh "mongodb+srv://..."    # Connect to MongoDB
db.users.find()               # List all users
db.users.deleteMany({})       # Clear users (CAREFUL!)

# TESTING
curl http://localhost:5000/api/health           # Health check
npm test                                        # Run tests (if exist)

# DEPLOYMENT
git push origin main                            # Auto-deploys via CI/CD
# Or manual via Render/Netlify dashboard

# MONITORING
tail -f logs/server.log                         # View backend logs
# Render: Dashboard → Logs
# Netlify: Site → Deploys → Logs
```

---

**Last Updated**: November 12, 2025  
**Document Version**: 1.0  
**For questions, refer to**: PRODUCTION_AUDIT_REPORT.md

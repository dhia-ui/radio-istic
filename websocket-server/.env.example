# WebSocket Server Environment Variables

# MongoDB Configuration (same database as backend-api)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/radio-istic?retryWrites=true&w=majority&appName=Cluster0

# Server Configuration
SOCKET_PORT=3001
PORT=3001
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,https://radioistic.netlify.app
